package troshx.util.layout;

/**
 * This provides a constraint somewhat similar objective to using Anchors to clamp layout borders to some edge, 
 * but doing so selectively for each cardinal side,
 * and with additional ability to set min/max relative scale margin range values for each side.
 * 
 * By default, will either clamp to or stretch to any activated side border.
 * 
 * @author Glidias
 */
class BorderConstraint 
{

	function new() 
	{
		
	}
	
}